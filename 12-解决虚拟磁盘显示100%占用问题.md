# 解决虚拟磁盘`/dev/sda2`显示100%占用问题

`/dev/sda2` 达到 100% 通常意味着你的**根目录（/）或主分区**已经完全塞满。这会导致系统无法启动图形界面、无法安装更新、甚至连简单的文件保存都会报错。  
※ 其实这是一个虚假问题，可能是虚拟机的一个 BUG 问题。在执行 `dd if=/dev/zero of=zero bs=4k; \rm zero` 指令的时候会出错。

以下是排查和解决这一问题的标准流程：

------

### 第一步：确认挂载点

首先要搞清楚 `/dev/sda2` 到底挂载在哪里。执行：

Bash

```
df -h
```

查看 `/dev/sda2` 对应的 **Mounted on（挂载点）**。

- 如果是 `/`：说明是系统核心分区满了。
- 如果是 `/home`：说明是你的个人资料（下载、视频等）占满了。

------

### 第二步：揪出“空间杀手” (排查大文件)

通常是某些日志文件或隐藏的缓存文件在作怪。我们可以使用 `du` 命令层层剥皮：

1. **查找根目录下占用最大的前 10 个目录：**

   Bash

   ```
   sudo du -ah / | sort -rh | head -n 10
   ```

   *(注意：这可能需要一点时间。如果你能确定大概位置，可以将 `/` 换成 `/var` 或 `/home`)*

2. **重点怀疑对象：**

   - **`/var/log`**：系统或应用的日志文件（如 `syslog` 或 `kern.log`）因为错误而疯狂增长。
   - **`/var/cache/apt/archives`**：下载的安装包没清理。
   - **`~/.local/share/Trash`**：回收站里的东西没彻底删除。
   - **`/var/lib/docker`**：如果你用 Docker，镜像和容器极其占空间。

------

### 第三步：快速止血 (常见清理方案)

如果系统已经卡死，先执行这几条指令强制腾出空间：

#### 1. 清理超大日志文件

如果发现 `/var/log` 下有巨大的文件（比如几个 GB 的 `syslog`），可以清空它（不要删除文件本身，清空内容即可）：

Bash

```
sudo truncate -s 0 /var/log/syslog
sudo truncate -s 0 /var/log/kern.log
```

#### 2. 清理 APT 缓存

Bash

```
sudo apt-get clean
```

#### 3. 删除旧版内核

Ubuntu 升级后会保留旧内核，如果 `/boot` 在该分区下，也会占空间：

Bash

```
sudo apt-get autoremove --purge
```

------

### 第四步：进阶排查——“空间去哪儿了？” (Inode 问题)

有时候 `df -h` 显示还有空间，但依然提示磁盘满，这可能是 **Inode（索引节点）用光了**。这通常是因为文件夹里产生了数以百万计的小文件（比如缓存碎片）。

- **检查 Inode 使用情况：**

  Bash

  ```
  df -i
  ```

  如果 `IUse%` 是 100%，你需要找到并删除包含大量小文件的文件夹。