# 监控磁盘的 **I/O** 性能

要监控磁盘的 **I/O（输入/输出）性能、读写速度、吞吐量以及是否达到了物理瓶颈（全速读写）**，前面提到的 `df` 或 `du` 就无能为力了，因为它们只能查看静态的“容量空间”。

在 Ubuntu 中，查看磁盘实时 I/O 情况最专业、最常用的工具是 **`iostat`** 和 **`iotop`**。以下是具体的使用方法，能帮你一眼看出磁盘是否在“疯狂飙车”：

## 工具一：`iostat` 

> <FONT COLOR=RED>**全面查看磁盘读写速率与瓶颈（最推荐）**</FONT>，新版 Ubuntu 中，该工具已经默认安装。

`iostat` 是系统性能监控的王牌工具。它能告诉你磁盘现在每秒读写多少兆字节（MB），以及磁盘有多忙。

### 1. 安装工具包

它包含在 `sysstat` 工具集中，需要先安装：

```bash
sudo apt update
sudo apt install sysstat -y
```

### 2. 核心监控命令

运行以下命令，每隔 **1 秒** 刷新一次数据，并以 **MB** 为单位显示：

```bash
iostat -x -m 1
```

**如何根据输出判断是否“全速读写”？** 输出中会列出你电脑上的所有磁盘（如 `sda`, `nvme0n1` 等），重点观察以下几个指标：

- **`rMB/s` 和 `wMB/s`：** 每秒写入和读取的数据量。如果这两个数值达到了你硬盘的物理极限（例如普通机械硬盘 100-150MB/s，普通 SATA SSD 500MB/s，NVMe M.2 SSD 2000-7000MB/s），那它就在全速读写。
- **`await`：** 每次 I/O 请求的平均等待时间（毫秒）。**如果这个数值突然飙升到几十甚至几百，说明磁盘已经忙不过来、出现严重堵塞了。**
- **`%util`（核心关键点）：** 磁盘用于处理 I/O 请求的时间百分比。**如果 `%util` 接近 100%，意味着磁盘在疯狂地满负荷运转，没有任何闲置时间！这就是你要找的“全速读写”的最直观证据。**

## 工具二：`iotop` 

> <FONT COLOR=RED>**抓出是哪个“幕后黑手”在疯狂读写磁盘**</FONT>

当你通过 `iostat` 确认磁盘利用率已经 100% 了，你肯定想知道**到底是哪一个软件或进程在疯狂读写它**。这时候就要用 `iotop`（它就像是磁盘读写版的“任务管理器”）。

### 1. 安装工具

```bash
sudo apt install iotop -y
```

### 2. 运行命令（必须用 sudo）

```bash
sudo iotop
```

*💡 提示：运行后，你可以按下键盘上的 **`o`** 键（Only），让它**只显示当前正在进行 I/O 活动的进程**，把那些静止不动的进程隐藏掉，界面会清晰很多。*

**如何看懂界面：**

- 在最顶部，你能看到系统整体的 **Total DISK READ**（总读取）和 **Total DISK WRITE**（总写入）速度。
- 在下方的列表中，**`DISK READ`** 和 **`DISK WRITE`** 两列会实时显示每个进程的具体读写速度。
- **`IO` 列（百分比）：** 显示该进程占用磁盘 I/O 资源的比例。排在最上面的那个进程，通常就是导致你磁盘全速运转的“罪魁祸首”。

## 工具三：`glances` —— 

> <FONT COLOR=RED>**懒人专属的“全家桶”监控大屏**</FONT>

如果你不想记那么多复杂的命令，想要一个把 CPU、内存、网络、以及**磁盘 I/O 读写速度**全部融合在一起的彩色大屏幕，`glances` 是极佳的选择。

### 1. 安装 glances

```bash
sudo apt install glances -y
```

### 2. 运行

```bash
glances
```

在 `glances` 界面的左侧或下方，会有一个 **DISK I/O** 区域，直观地列出每个盘符当前的读（`R`）和写（`W`）的速度。如果某项指标过高，它还会自动用**红色**或**黄色**进行高亮警告。

## 总结排查思路：

1. 先用 `iostat -x -m 1` 观察 **`%util`** 是否达到了 **100%**，确认磁盘是否真的在全速读写。
2. 一旦确认被占满，立刻开一个新窗口运行 `sudo iotop -o`，直接就能揪出是哪个进程（比如是不是 Git 在扫描、某个数据库在频繁写入、或者系统在疯狂读写 Swap 交换分区）把磁盘拖卡顿的。



#  %idle 和 %iowait 大约各占 50%

如果你在终端里看到 **`%idle` 和 `%iowait` 大约各占 50%**（或者两者加起来接近 100%），这是一个非常经典、教科书般的“磁盘 I/O 瓶颈”**或**“单线程并发 I/O 阻塞”场景。

用一句话来概括就是：**此时你的 CPU 有一半的时间在绝对空闲（躺平），而另一半的时间在苦苦等待磁盘读写完成（卡死）。**

为了彻底搞懂这是怎么回事，我们需要拆解这 50对50 的背后到底发生了什么：

## 1. 为什么两者的比例是 50% vs 50%？

这通常是由**单线程（或单进程）进行高强度的、连续的同步磁盘读写**引起的。

假设你的 Ubuntu 服务器是 **双核（2核）CPU**，或者某个关键任务只用到了其中 **1 个 CPU 核心**：

- **1 个核心在疯狂处理磁盘数据：** 当它发出读取大文件的指令后，磁盘速度太慢，这个 CPU 核心什么也干不了，只能原地抱头等待数据传输过来。这个核心的全部时间都会被计入 **`%iowait`**。因为一个核心占了总 CPU 资源的 50%，所以系统整体显示 `%iowait ≈ 50%`。
- **另一个核心完全没事干：** 系统里没有其他繁重的计算任务，另外 1 个核心处于绝对的清闲状态。这个核心的时间会被计入 **`%idle`**。由于它也占总资源的 50%，所以系统整体显示 `%idle ≈ 50%`。

$$\text{总 CPU (100\%)} = \underbrace{\text{核1: 苦等磁盘 (100\% iowait)}}_{\text{贡献了全局 50\% iowait}} + \underbrace{\text{核2: 拍苍蝇躺平 (100\% idle)}}_{\text{贡献了全局 50\% idle}}$$

如果是多核（比如 4 核、8 核），代表有其中几个核心被磁盘等待给完全“绑架”了，而剩下的核心在大是大非面前依然选择无所事事。

## 2. 这时候系统的真实状态是怎样的？

- **磁盘角度：** 磁盘此时**大概率正在全速运转**，或者处理着海量的碎片小文件（导致磁头频繁寻道，效率极低）。
- **应用角度：** 那个正在读写磁盘的程序（比如正在进行大文件拷贝、执行没有索引的复杂数据库查询、或者是乌龟Git正在深度遍历整个超大代码库）**运行得非常慢**。
- **系统整体：** 此时你如果操作电脑（比如在资源管理器里点击文件夹，或者在终端敲命令），可能会感觉到明显的**顿挫感或卡顿**。

## 3. 常见引发这种现象的“幕后黑手”

1. **乌龟 Git（TortoiseGit）或类似的本地同步工具：** 正在后台对大体量的代码库或者成千上万个小文件进行全盘状态扫描（`TSVNCache.exe` 在疯狂工作）。
2. **正在解压/压缩大文件：** 比如你在执行 `tar -xvf` 解压一个大压缩包，硬盘写入速度跟不上解压速度。
3. **数据库在做全表扫描（Full Table Scan）：** MySQL 或 PostgreSQL 正在读取没有建索引的数据，被迫把整块硬盘的数据都读进内存。
4. **内存不足，系统在疯狂使用 Swap（交换分区）：** 内存满了，Linux 迫不得已把硬盘当内存用（疯狂进行 Page In/Page Out 换页操作），由于硬盘比内存慢得多，CPU 瞬间被 `iowait` 淹没。

## 4. 下一步该如何排查？

既然你抓到了这个 50/50 的现行，千万不要关掉终端，立刻执行下面这个命令去抓出那个导致 `%iowait` 达到 50% 的具体进程：

```bash
sudo iotop -o
```

打开 `iotop` 后，看看排在最前面的那个 **`IO>` 接近或等于 99%** 的进程是谁。那个进程，就是把你的 CPU 强行拖进“无尽等待”的始作俑者。

你可以运行 `iotop` 看看，目前排在最前面的那个进程名字叫什么？