# 检查磁盘健康状况

## 1. 命令行工具 `smartmontools`

`smartctl` 是 Linux 下最标准、功能最强大的磁盘检测工具（最全面，适合服务器与终端）。

**1. 安装工具**

```bash
sudo apt update
sudo apt install smartmontools -y
```

**2. 查看磁盘列表并确认盘符**

```bash
sudo lsblk
```

*(假设你要检查的 1TB 机械硬盘盘符为 `/dev/sda`)*

**3. 一键查看综合健康结果**

```bash
sudo smartctl -H /dev/sda
```

- **健康输出**：`SMART overall-health self-assessment test result: PASSED`（表示磁盘状态正常）
- **警告输出**：如果显示 `FAILED`，说明硬件检测到坏道或故障，必须立即备份数据并更换硬盘。

**4. 运行深度检测（自检命令）** 如果想让硬盘自我测试是否有坏道：

```bash
# 执行快速自检（约需 2 分钟）
sudo smartctl -t short /dev/sda

# 等待 2 分钟后查看自检报告结果
sudo smartctl -l selftest /dev/sda
```

**5. 坏道与重映射数据**

如果你想完全放心，可以再看看这块盘的具体**坏道与重映射数据**，执行以下命令查看核心 SMART 属性：

```bash
sudo smartctl -A /dev/sda | grep -E "Reallocated_Sector|Current_Pending|Offline_Uncorrectable"
```

※ 只要输出中的 `RAW_VALUE`（最右侧一列的数值）全部为 **0**，就说明这块 1TB 机械硬盘完全健康，可以放心使用。



## 2. 深度检查

如果你想在 Linux 下做一次像 DiskGenius 那样彻底的**全盘坏道扫描**，可以使用以下两种方法（同样需要几小时）：

**方法一：运行 SMART 深度自检 (`long` 测试)** 这是由硬盘硬件自己后台全盘扫描，不占用 CPU：

```bash
sudo smartctl -t long /dev/sda
```

*(查看时间大约需要 120 - 200 分钟，期间电脑可以正常使用)*

**方法二：使用 `badblocks` 命令行工具** 直接逐块读取扫描，最接近 DiskGenius 的底层检测：

```bash
# 只读模式全盘检测坏道并显示进度
sudo badblocks -s -v /dev/sda
```

**总结**： 日常维护看 `smartctl` 打印的日志就完全够用（主控一旦发现坏道会自动登记在案）；只有在怀疑硬盘有严重读取卡顿、但 S.M.A.R.T. 还没报错时，才需要用 DiskGenius 或 `badblocks` 做全盘慢速扫盘。