# 删除旧内核

在 Ubuntu 中，系统在更新时会自动下载新内核，但通常**不会主动删除旧内核**，以防新内核出现兼容性问题时可以回滚。久而久之，这些旧内核会占用大量的 `/boot` 分区或系统盘空间。

以下是安全删除 Ubuntu 旧内核的几种方法，按**推荐程度**排序：

------

### 方法一：使用 `apt autoremove`（最安全、最推荐）

现代的 Ubuntu 系统（Ubuntu 16.04 及更高版本）已经非常智能。系统会自动保留当前正在使用的内核以及上一个版本作为备份，其余的旧内核可以通过一行命令自动识别并安全清理。

打开终端，运行以下命令：

```bash
sudo apt autoremove --purge -y
```

- **作用**：`--purge` 参数会连同内核的配置文件一起彻底删除。这个命令绝对安全，它绝对不会删除你正在使用的内核。

------

### 方法二：使用 `unattended-upgrades` 实现自动清理（一劳永逸）

如果你希望系统以后在无人值守更新时，自动清理旧内核，可以修改系统配置。

1. 打开配置文件：

   ```bash
   sudo nano /etc/apt/apt.conf.d/50unattended-upgrades
   ```
   
2. 在文件中找到以下两行（如果前面有 `//` 注释符号，请删掉 `//`），并将值改为 `"true"`：

   ```Plaintext
   Unattended-Upgrade::Remove-Unused-Kernel-Packages "true";
   Unattended-Upgrade::Remove-New-Unused-Dependencies "true";
   ```
   
3. 保存退出（`Ctrl + O` 然后 `Ctrl + X`）。这样以后系统会自动帮你保持内核的整洁。

------

### 方法三：手动精准删除（适合磁盘完全爆满、自动命令失效时）

有时候 `/boot` 分区甚至爆满到 100%，导致 `apt autoremove` 报错无法运行。这时你需要手动介入。

#### 第一步：查看当前正在使用的内核

**千万不要删除这个版本的内核！**

```bash
uname -r
```

*输出示例：`5.15.0-60-generic`*

#### 第二步：列出系统中安装的所有内核

```bash
dpkg --list | grep linux-image
```

你会看到一长串列表。状态为 `ii` 表示已安装，而形如 `linux-image-5.15.0-58-generic` 就是旧内核。

#### 第三步：手动删除指定的旧内核

结合第二步看到的旧版本号（确认不是第一步运行 `uname -r` 得到的当前内核），运行以下命令彻底删除：

```bash
sudo apt-get purge linux-image-5.15.0-58-generic linux-headers-5.15.0-58-generic
```

*(请将 `5.15.0-58-generic` 替换为你系统里实际存在的旧内核版本号)*

#### 第四步：更新引导菜单

手动删除后，建议刷新一下 GRUB 开机引导菜单：

```bash
sudo update-grub
```

------

### 🚨 注意事项

1. **至少保留两个内核**：系统通常会保留当前工作的内核（Current）和一个备用内核（Old/Backup）。如果你手动删除，请务必留下一个可以正常启动的备用内核，以防万一。
2. **清理后检查空间**：清理完毕后，可以使用 `df -h /boot` 命令查看 `/boot` 分区的释放情况。