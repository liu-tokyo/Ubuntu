# 磁盘清理脚本

### 1. 快速清理命令（无需建文件）

如果你只想临时清理一下，直接在终端（Terminal）中复制并运行这一行组合命令即可：

```bash
sudo apt-get clean && sudo apt-get autoclean && sudo apt-get autoremove -y && rm -rf ~/.cache/thumbnails/*
```

------

### 2. Ubuntu 磁盘清理一键脚本（.sh）

如果你想以后长久使用，可以创建一个脚本文件。

#### 脚本代码 (`clean_disk.sh`)：

```bash
#!/bin/bash

# 确保以 root 权限运行
if [ "$EUID" -ne 0 ]; then
  echo "请使用 sudo 运行此脚本！"
  exit 1
fi

echo "==================== 开始清理 Ubuntu 磁盘空间 ===================="

# 1. 清理 APT 缓存的软件安装包
echo "--> 正在清理已下载的软件安装包缓存..."
apt-get clean
apt-get autoclean -y

# 2. 卸载孤立的、不再需要的依赖包
echo "--> 正在卸载不再需要的依赖软件包..."
apt-get autoremove -y

# 3. 清理旧版本的 Snap 软件包（Snap 默认会保留旧版本，非常占空间）
echo "--> 正在清理旧版本的 Snap 软件包..."
set -e
snap list --all | awk '/disabled/{print $1, $3}' |
    while read snapname revision; do
        snap remove "$snapname" --revision="$revision"
    done
set +e

# 4. 清理系统日志（保留最近 3 天的日志）
echo "--> 正在清理系统日志文件（仅保留最近3天）..."
if command -v journalctl &> /dev/null; then
    journalctl --vacuum-time=3d
fi

# 5. 清理当前用户的缩略图缓存
echo "--> 正在清理用户缩略图缓存..."
rm -rf /home/*/.cache/thumbnails/*

echo "==================== 磁盘清理完成！ ===================="
df -h | grep '^/dev/'
```

------

### 3. 如何使用这个脚本？

请按照以下步骤操作：

1. **创建脚本文件**： 在终端中运行以下命令，使用 `nano` 编辑器创建文件：

   ```bash
   nano clean_disk.sh
   ```
   
2. **粘贴代码**： 将上面的脚本代码复制，然后在终端中粘贴（快捷键通常是 `Ctrl + Shift + V`）。

3. **保存并退出**： 按下 `Ctrl + O` 保存，按 `Enter` 确认，然后按 `Ctrl + X` 退出编辑器。

4. **赋予执行权限**：

   ```bash
   chmod +x clean_disk.sh
   ```
   
5. **运行脚本**（由于涉及系统文件清理，必须加 `sudo`）：

   ```bash
   sudo ./clean_disk.sh
   ```

------

### 💡 进阶小贴士

- **大文件排查**：如果清理完空间还是不够，可以运行 `sudo apt install ncdu` 安装一个磁盘分析工具。安装后运行 `sudo ncdu /`，它会用直观的列表告诉你到底是哪个文件夹占用了最多的空间。
- **Docker 占用**：如果你平时使用 Docker，它往往是磁盘积压的“元凶”。可以使用 `sudo docker system prune -a --volumes` 来清理无用的 Docker 镜像和缓存（执行前请确保重要的容器已启动或已备份）。